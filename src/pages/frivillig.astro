---
import MainLayout from "../layouts/MainLayout.astro";
---
<MainLayout title="Bliv frivillig">
    <main>
        <section class="section">
            <div class="container-site flex justify-center">
                <form id="volunteerForm" class="card overflow-hidden max-w-2xl w-full">
                    <!-- Pink header -->
                    <div class="bg-pink-600 p-6 mb-6 -m-6 mb-6">
                        <h2 class="text-white font-bold t-h2">Bliv frivillig</h2>
                        <p class="text-white t-h3 mt-2">
                        Du behøver ikke special erfaring; du skal blot have tid, empati og lyst til at være der for andre. Vi sørger for oplæring og giver dig alt, du skal bruge for at kunne hjælpe trygt og sikkert.                        </p>
                    </div>

                    <!-- Formular indhold -->
                    <div class="space-y-4">
                        <p class="text-sm font-semibold text-gray-700">Udyld formularen, så vender vi tilbage til dig så hurtigt muligt</p>

                        <div class="grid grid-cols-1 gap-4">
                            <input type="text" placeholder="Fornavn" class="border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500" />
                            <input type="text" placeholder="Efternavn" class="border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500" />
                        </div>

                        <input type="email" placeholder="E-mail" class="w-full border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500" />

                        <input type="tel" placeholder="Telefonnummer" class="w-full border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500" />

                        <div class="space-y-2">
                            <label class="text-sm font-semibold text-gray-700">Fødseldsdag:</label>
                            <div class="grid grid-cols-3 gap-3">
                                <select class="border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500">
                                    <option>Dag</option>
                                    {Array.from({ length: 31 }, (_, i) => (
                                        <option>{i + 1}</option>
                                    ))}
                                </select>
                                <select class="border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500">
                                    <option>Måned</option>
                                    {["Januar", "Februar", "Marts", "April", "Maj", "Juni", "Juli", "August", "September", "Oktober", "November", "December"].map((month, index) => (
                                        <option>{month}</option>
                                    ))}
                                </select>
                                <select class="border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500">
                                    <option>År</option>
                                    {Array.from({ length: 26 }, (_, i) => (
                                        <option>{1980 + i}</option>
                                    ))}
                                </select>
                            </div>
                        </div>

                        <div class="space-y-2">
                            <label class="text-sm font-semibold text-gray-700">Motiverende ansøgning:</label>
                            <textarea placeholder="Kort tekst om dig selv og hvorfor du vil være frivillig..." class="w-full border border-pink-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-pink-500 h-32"></textarea>
                        </div>

                        <label class="flex items-center gap-2 text-sm text-gray-700">
                            <input type="checkbox" class="w-4 h-4 border border-pink-300 rounded" />
                            <span>I må gerne kontakte mig med information om næste skridt i frivilligprocessen</span>
                        </label>

                        <button type="submit" class="w-full bg-gradient-to-r from-pink-500 to-pink-600 text-white font-semibold py-3 rounded-lg hover:opacity-90 transition">
                            Send ansøgning
                        </button>
                    </div>
                </form>

                <!-- Tak-siden (skjult) -->
                <div id="thankYou" class="card overflow-hidden max-w-2xl w-full hidden">
                    <div class="bg-pink-600 p-6 mb-6 -m-6 mb-6">
                        <h2 class="text-white font-bold text-2xl">Tak for din ansøgning!</h2>
                    </div>
                    <div class="space-y-4">
                        <p class="text-gray-800 text-lg">
                            Vi er rigtig glade for din interesse i at blive frivillig hos Gå Med Mig.
                        </p>
                        <p class="text-gray-700">
                            Din ansøgning er modtaget, og vi vender tilbage til dig hurtigst muligt med mere information om næste trin i processen.
                        </p>
                        <p class="text-gray-700">
                            Hvis du har spørgsmål, kan du altid skrive til gaamedmigfrivillig@gmail.com.
                        </p>
                        <div class="pt-4">
                            <a href="/" class="inline-block bg-pink-600 text-white font-semibold py-3 px-6 rounded-lg hover:opacity-90 transition">
                                Tilbage til forsiden
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
</MainLayout>

<script>
    const form = document.getElementById('volunteerForm');
    const thankYou = document.getElementById('thankYou');

    form.addEventListener('submit', (e) => {
        e.preventDefault();
        form.classList.add('hidden');
        thankYou.classList.remove('hidden');
    });
</script>